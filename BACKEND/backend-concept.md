FRONTEND => HTTP REQUEST => REST API => BACKEND LOGIC => RESPONSE => FRONTEND

\*\*WHY DID I DO THIS?
-Because everything im going to build in the backend is going to follow this pipeline.

#FOLDER STRUCTURE
